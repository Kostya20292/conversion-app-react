# План реализации фронтенда: Convertly (v1)

Пошаговый план для `apps/web`. Источники правды:

| Документ                                                           | Что брать                                                              |
| ------------------------------------------------------------------ | ---------------------------------------------------------------------- |
| [technical-task.md](./technical-task.md)                           | Экраны §4, ошибки §5, auth §6, jobs/polling §7, UC §12                 |
| [architecture.md](./architecture.md)                               | Структура `apps/web` §3.1, поток UI §4.1                               |
| [tech-stack.md](./tech-stack.md)                                   | React, Vite, SCSS Modules, Zustand, react-dropzone, Vitest, Playwright |
| [DESIGN.md](./DESIGN.md)                                           | Токены, тёмная тема, компоненты, Do/Don't                              |
| [backend-implementation-plan.md](./backend-implementation-plan.md) | HTTP-контракт §12; этапы Nest A–I; что разблокирует фронт B–G          |

Стек и границы v1 **фиксированы** — альтернативы не предлагать. Бизнес-логика конвертации только на
API.

**Прогресс:** этапы **A**, **A2** и **B** закрыты (UI без Nest + HTTP-клиент + сессия cookie +
guard). `/api-docs` и часть unit — по-прежнему пропущены. Nest **§7–§8** (jobs, worker, signed
download) закрыты. Дальше — этап **C** (upload / poll / download на главной)
([backend-implementation-plan.md](./backend-implementation-plan.md) §8, §16).

---

## 0. Исходное состояние

Уже есть: Vite + React 19, TypeScript, alias `@/`, SCSS Modules, React Router, Zustand,
react-dropzone, Vitest, Playwright, Oxlint, React Compiler. Каркас папок (`pages/`,
`features/conversion/`, `api/`, …). Шаблон Vite заменён на SPA-shell (этап A).

**Цель плана:** заменить шаблон на SPA по ТЗ, слой за слоем, с проверяемым результатом на каждом
этапе.

**Зависимость от backend:** план API —
[backend-implementation-plan.md](./backend-implementation-plan.md). Пока Nest нет — **не** пишем
`src/api/`, **не** мокаем эндпоинты (ни адаптер, ни MSW). Делаем только UI и клиентскую логику.
Публичный `X-API-Key` в SPA **не** кладём ([architecture.md](./architecture.md) §3.1). Пути UI и
`/api/v1` — канон в бэкенд-плане §12, handshake — §17.

---

## 0.1. Текущий фокус: гостевая конвертация на главной

Сессия SPA живая. Nest POST/GET jobs, worker и signed download готовы. На главной CTA ещё не шлёт
файл на API. Моки HTTP **не** делаем.

| #   | Что делать сейчас                                                   | Где в плане        | Статус      |
| --- | ------------------------------------------------------------------- | ------------------ | ----------- |
| 1   | `Dropzone` + выбранный файл: Idle → Drag-over → Selected → Error    | §3 #8, §7, §12     | ✅          |
| 2   | Клиентская валидация файла: 1 шт., ≤10 МБ, расширение, не 0 байт    | §6.2               | ✅          |
| 3   | Zustand: `route` / `file` / `error` (без `job` и polling)           | §6.1 (часть), §6.7 | ✅          |
| 4   | Главная: Dropzone вместо заглушки; CTA disabled без валидного файла | §7                 | ✅          |
| 5   | Валидация `/login` и `/register` (пароль ≥8, буква + цифра)         | §5.3–5.4           | ✅          |
| 6   | Валидация `/forgot-password` и `/reset-password`                    | §10.1, §10.3       | ✅          |
| 7   | Дособрать UI ЛК без данных (профиль, ключ, списки, empty)           | §9                 | ✅          |
| 8   | `/s/:token`: метаданные + вёрстка «ссылка недоступна»               | §8.2–8.3           | ✅          |
| 9   | HTTP-клиент + сессия cookie + guard `/account`                      | §4, §5             | ✅          |
| 10  | Полные `/api-docs`: эндпоинты, curl + fetch, ошибки, лимиты         | §11                | ⏸ пропущено |
| 11  | Unit: валидация файла и правила пароля                              | §13                | ⏸ пропущено |
| 12  | Upload + poll + download на главной (гость)                         | §6.3–6.6, §7       | ⬜          |

**Дальше фронт C:** §6.3–6.6 upload / poll / download. Позже: share create (бэкенд D), живые данные
ЛК кроме ключа/сессии (бэкенд E), recovery (бэкенд G). Соответствие этапов —
[backend-implementation-plan.md](./backend-implementation-plan.md) §16.

---

## 1. Фундамент: токены, шрифты, layout-shell

**Зачем:** единый визуальный базис до любых экранов ([DESIGN.md](./DESIGN.md)).

| Шаг | Действие                                                                                                            | Критерий готовности               | Статус |
| --- | ------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ------ |
| 1.1 | CSS-переменные в `src/styles/` из DESIGN (colors, spacing, rounded, typography)                                     | Переменные подключены глобально   | ✅     |
| 1.2 | Подключить Manrope + JetBrains Mono                                                                                 | Шрифты на `body` / code-блоках    | ✅     |
| 1.3 | Reset / base (`index.css` или `styles/global.scss`): canvas `#0B0F14`, текст `on-surface`, контейнер max **1120px** | Страница тёмная, без Vite-шаблона | ✅     |
| 1.4 | Удалить демо-ассеты Vite из UI (`App.css`, логотипы-счётчик)                                                        | Чистый entry                      | ✅     |

**Вне scope этапа:** бизнес-страницы.

---

## 2. Каркас приложения: роутер, Header, Footer

**Зачем:** общий каркас всех страниц ([technical-task.md](./technical-task.md) §4.1).

| Шаг | Действие                                                                                                                                    | Критерий готовности                                                              | Статус |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- | ------ |
| 2.1 | `app/router` + маршруты: `/`, `/login`, `/register`, `/forgot-password`, `/reset-password`, `/account`, `/s/:token`, `/api-docs`, `*` (404) | Навигация по URL без 404 на известных путях; неизвестные — страница «не найдена» | ✅     |
| 2.2 | `AppLayout`: Header + `<Outlet />` + Footer                                                                                                 | Все страницы в одном каркасе                                                     | ✅     |
| 2.3 | Header: логотип → `/`; «Конвертация», «API»; «Войти» / «ЛК» (из сессии)                                                                     | Соответствует §4.1 и DESIGN                                                      | ✅     |
| 2.4 | Footer: `© Convertly 2026 — конвертация файлов онлайн`                                                                                      | Как в ТЗ                                                                         | ✅     |
| 2.5 | Placeholder-страницы с заголовком                                                                                                           | Можно кликать по меню                                                            | ✅     |

**Маршруты (сводка):**

| Path                                  | Page                |
| ------------------------------------- | ------------------- |
| `/`                                   | Home                |
| `/login`, `/register`                 | Auth                |
| `/forgot-password`, `/reset-password` | Recovery            |
| `/account`                            | Account (protected) |
| `/s/:token`                           | Share (public)      |
| `/api-docs`                           | ApiDocs (public)    |

---

## 3. UI-kit: переиспользуемые компоненты

**Зачем:** одни примитивы для форм, CTA, dropzone, модалок ([DESIGN.md](./DESIGN.md) Components).

Порядок сборки (от простого к сложному):

| #   | Компонент                     | Обязательные состояния / a11y                                                     | Статус |
| --- | ----------------------------- | --------------------------------------------------------------------------------- | ------ |
| 1   | `Button`                      | primary / secondary / tertiary / danger; disabled; height 44px; не `rounded.full` | ✅     |
| 2   | `Input`, `Checkbox`, `Toggle` | focus-ring, `aria-invalid`, `aria-describedby`                                    | ✅     |
| 3   | `Alert` / field error         | ошибки рядом с зоной действия, RU                                                 | ✅     |
| 4   | `SegmentedControl`            | 4 направления: JPG→PNG, PNG→JPG, DOCX→PDF, PDF→DOCX                               | ✅     |
| 5   | `Modal`                       | focus-trap, Escape, overlay                                                       | ✅     |
| 6   | `Toast` / `Banner`            | 5xx, гостевой баннер                                                              | ✅     |
| 7   | `Spinner` / linear progress   | статусы job                                                                       | ✅     |
| 8   | `Dropzone` (+ selected file)  | Idle → Drag-over → Selected → Error; `aria-label`; 1 файл, ≤10 МБ                 | ✅     |

UI-kit v1 без Nest закрыт.

Стили — **SCSS Modules**. Карточки только где нужна интерактивная группа (auth, ЛК-списки), не в
hero.

**Критерий:** Story-less smoke: страница-песочница или временный блок на Home показывает все
варианты кнопок/инпутов. → ✅ закрыто использованием компонентов на реальных страницах (без
отдельной песочницы).

---

## 4. HTTP-клиент и типы API

**Когда:** после бэкенд-этапа **B**
([backend-implementation-plan.md](./backend-implementation-plan.md) §4, §16). Закрыто вместе с
фронтовым B.

**Зачем:** тонкий слой `src/api/` ([architecture.md](./architecture.md) §3.1).

| Шаг | Действие                                                                                       | Критерий готовности                                                    | Статус      |
| --- | ---------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | ----------- |
| 4.1 | Базовый `fetch` с `credentials: 'include'` (cookie JWT); Vite proxy `/api` → Nest              | Cookie уходит на `/api/auth/*` и `/api/jobs`                           | ✅          |
| 4.2 | Типы ответов: Job, User, StoredFile, ShareLink, ApiError                                       | Без лишнего `any`                                                      | ✅          |
| 4.3 | Маппинг `error.code` → русские сообщения ([technical-task.md](./technical-task.md) §5.1, §7.5) | UI никогда не показывает сырой английский `message` как основной текст | ✅          |
| 4.4 | Обработка 401 (сессия), 429 (rate limit), 5xx (toast)                                          | Единая точка                                                           | ✅          |
| 4.5 | Мок-адаптер / MSW                                                                              | **Не делаем.** Фронт без Nest идёт по §0.1, без фейкового API          | ⏸ не делаем |

**Контракт UI** — канон в [backend-implementation-plan.md](./backend-implementation-plan.md) §12 (не
дублировать пути здесь). Кратко:

| Область       | Префикс (бэкенд §12)                                          |
| ------------- | ------------------------------------------------------------- |
| Auth          | `/api/auth/register`, `login`, `logout`, `me`                 |
| Jobs (UI)     | `/api/jobs` — multipart, GET status, signed download          |
| Account       | `/api/users/me`, `/api/api-keys`, `/api/files`, `/api/shares` |
| Public share  | `/api/v1/public/s/:token`                                     |
| Публичный API | `/api/v1/*` + `X-API-Key` — **не** из SPA                     |

Polling: **каждые 2 с**, пока `queued` \| `processing`; стоп на `completed` / `failed` или таймаут
60 с ([technical-task.md](./technical-task.md) §7.2, §13 #23). Handshake (cookie, proxy, конверт
ошибок) — бэкенд-план §17.

---

## 5. Auth: сессия, guard, формы входа/регистрации

**Зачем:** ЛК и персональные настройки ([technical-task.md](./technical-task.md) §4.3, §6). Живая
сессия — после бэкенд-B ([backend-implementation-plan.md](./backend-implementation-plan.md) §4).

| Шаг | Действие                                                                   | Nest? | Критерий готовности                | Статус |
| --- | -------------------------------------------------------------------------- | ----- | ---------------------------------- | ------ |
| 5.1 | Zustand (или лёгкий auth-store): `user \| null`, `status`                  | да    | Header переключается Войти ↔ ЛК    | ✅     |
| 5.2 | `GET /api/auth/me` при старте приложения                                   | да    | Гидратация сессии                  | ✅     |
| 5.3 | `/login`: email, пароль, «Запомнить меня», ссылки forgot/register          | нет   | Чекбокс + **клиентская** валидация | ✅     |
| 5.4 | `/register`: имя, email, пароль + подтверждение; CTA Telegram «позже в ЛК» | нет   | Валидация: ≥8, буква + цифра       | ✅     |
| 5.5 | Ошибки: «Неверный email или пароль»; занятый email + ссылка на login       | да    | Без enumeration по полям на login  | ✅     |
| 5.6 | После register → автологин → редирект `/account`                           | да    | UC-02                              | ✅     |
| 5.7 | Protected route `/account`: редирект `/login?next=...`                     | да    | §5.1 матрица                       | ✅     |
| 5.8 | Logout                                                                     | да    | Инвалидация + UI guest             | ✅     |

**Сейчас:** сессия живая (register → account → logout → login). `/api-docs` и часть unit —
пропущены.  
**Критерий после бэкенд-B:** ручной проход register → account → logout → login — выполнен.

---

## 6. Ядро конвертации: `features/conversion`

**Зачем:** одна операция = один файл; UI через jobs + polling ([architecture.md](./architecture.md)
§4.1). Upload/poll/download на главной — этап C; Nest jobs/worker/download уже есть (§7–§8).

| Шаг | Действие                                                                       | Nest? | Критерий готовности              | Статус |
| --- | ------------------------------------------------------------------------------ | ----- | -------------------------------- | ------ |
| 6.1 | Zustand store: route / file / error; `job` — после бэкенд-C                    | часть | Одно активное состояние операции | ✅     |
| 6.2 | Клиентская валидация до upload: 1 файл, ≤10 МБ, расширение под пару, не 0 байт | нет   | UC-05; тексты из §5.1            | ✅     |
| 6.3 | Upload multipart + старт job                                                   | да    | Переход Selected → Uploading     | ⬜     |
| 6.4 | Polling 2 с                                                                    | да    | Processing с индикатором         | ⬜     |
| 6.5 | Success: `download_url`, кнопки Скачать / Поделиться                           | да    | Состояния §5.2                   | ⬜     |
| 6.6 | Failed / timeout / network: RU + Retry                                         | да    | Матрица §5.1                     | ⬜     |
| 6.7 | Сброс / заменить файл                                                          | нет   | Возврат к Idle/Selected          | ✅     |

**Направления v1:** JPG→PNG, PNG→JPG, DOCX→PDF, PDF→DOCX. Отдельных URL под типы нет — только
сегмент на главной.

---

## 7. Главная страница `/`

**Зачем:** весь happy-path гостя ([technical-task.md](./technical-task.md) §4.2, UC-01).

| Блок                      | Реализация                                                          | Nest? | Статус |
| ------------------------- | ------------------------------------------------------------------- | ----- | ------ |
| Hero                      | Бренд **Convertly** (hero-уровень) + короткий подзаголовок          | нет   | ✅     |
| SegmentedControl          | 4 направления: `JPG → PNG`, `PNG → JPG`, `DOCX → PDF`, `PDF → DOCX` | нет   | ✅     |
| Dropzone + выбранный файл | Компонент из §3 + store `file`/`error` из §6.1–6.2                  | нет   | ✅     |
| CTA «Конвертировать»      | Primary; disabled без валидного файла                               | нет   | ✅     |
| Статус / результат        | Progress → Скачать / Поделиться                                     | да    | ⬜     |
| Баннер гостю              | «Войдите, чтобы сохранять файлы…»                                   | нет   | ✅     |
| Блок API                  | Кратко + ссылки `/api-docs`, «Получить ключ» → register/account     | нет   | ✅     |
| «Как это работает»        | 3 шага; **ниже** первого viewport                                   | нет   | ✅     |

Первый viewport: бренд + подзаголовок + сегмент + dropzone + один primary CTA. Без статистики и
конкурирующих CTA ([DESIGN.md](./DESIGN.md) Do/Don't).

**Сейчас:** Dropzone + валидация + CTA; гостевой баннер скрывается после входа. Nest jobs/worker
готовы; CTA ещё заглушка. **Этап C:** гость конвертирует один файл end-to-end
([backend-implementation-plan.md](./backend-implementation-plan.md) §6–§8).

---

## 8. Шаринг: кнопка + страница `/s/:token`

**Зачем:** UC-06, §4.6, §9. Создать ссылку — после бэкенд-D
([backend-implementation-plan.md](./backend-implementation-plan.md) §10).

| Шаг | Действие                                                         | Nest? | Критерий готовности                      | Статус |
| --- | ---------------------------------------------------------------- | ----- | ---------------------------------------- | ------ |
| 8.1 | После success: «Поделиться» → создать share → копировать URL     | да    | Работает для гостя (share на job) и user | ⏸      |
| 8.2 | Страница `/s/:token`: имя, формат, размер, TTL, Скачать          | нет   | Вёрстка метаданных (без живых данных)    | ✅     |
| 8.3 | Состояния `410` / отозвана / истекла: «Ссылка больше недоступна» | нет   | Вёрстка без деталей владельца            | ✅     |
| 8.4 | CTA «Сконвертировать свой файл» → `/`                            | нет   |                                          | ✅     |

Пароля на ссылку нет.

**Сейчас (без Nest):** вёрстка доступной ссылки со статическими метаданными; `/s/gone`, `/s/expired`
и `/s/revoked` показывают «Ссылка больше недоступна» без деталей владельца. Создание share и живые
данные — после бэкенд-D ([backend-implementation-plan.md](./backend-implementation-plan.md) §10).

---

## 9. Личный кабинет `/account`

**Зачем:** §4.5, UC-02, UC-03.

Порядок блоков на странице:

| #   | Блок                      | Ключевое поведение                                                       | Nest? | Статус   |
| --- | ------------------------- | ------------------------------------------------------------------------ | ----- | -------- |
| 1   | Профиль                   | Имя; email (пароль для смены); пароль; Telegram bind/unbind (UI)         | нет   | ✅       |
| 2   | API-ключ                  | Показ plaintext после register; маска с API после login; перевыпуск — UI | да    | 🟡       |
| 3   | Toggle `save_conversions` | Default `false`; выкл не удаляет уже сохранённые                         | нет   | ✅       |
| 4   | Список StoredFile         | Имя, тип, размер, дата, источник UI/API; скачать / поделиться / удалить  | нет   | ✅ empty |
| 5   | Активные ShareLink        | URL, срок, файл; копировать / отозвать                                   | нет   | ✅ empty |
| 6   | Empty state               | Текст-онбординг без коллажей                                             | нет   | ✅       |
| 7   | Выход                     | Кнопка вызывает `POST /api/auth/logout`; UI гостя                        | да    | ✅       |

Layout: mobile — одна колонка; desktop — настройки сверху/слева, списки ниже/справа, без
дашборд-перегруза.

**Критерий после бэкенд-E:** включить save → конвертация → файл в списке; отозвать share →
`/s/:token` недоступна ([backend-implementation-plan.md](./backend-implementation-plan.md) §5, §9).

**Сейчас:** сессия и выход живые; после register plaintext ключа один раз, после login — маска с
`GET /api/api-keys`. Профиль/Telegram/перевыпуск/toggle — UI без записи на сервер. Списки пустые.
Живые files/shares — после бэкенд-E.

---

## 10. Восстановление пароля

**Зачем:** §4.4, UC-04. Живой Telegram Bot API — вне v1 ([tech-stack.md](./tech-stack.md) §6). Код
сброса — бэкенд-этап **G** ([backend-implementation-plan.md](./backend-implementation-plan.md) §11);
сейчас — формы и клиентская валидация.

| Шаг  | Страница           | Поведение                                       | Nest? | Статус |
| ---- | ------------------ | ----------------------------------------------- | ----- | ------ |
| 10.1 | `/forgot-password` | Форма email; нейтральный ответ — после бэкенд-G | нет   | ✅     |
| 10.2 | UX ожидания        | Инструкция открыть Telegram-бота                | нет   | ✅     |
| 10.3 | `/reset-password`  | Код + новый пароль; клиентская валидация пароля | нет   | ✅     |
| 10.4 | Успех              | Редирект на login                               | да    | ⏸      |

**Сейчас (без Nest):** §10.1–10.3 закрыты — валидация email/кода/пароля, submit никуда не уходит.  
**После бэкенд-G:** отправка кода, проверка TTL, редирект на login.

---

## 11. API docs `/api-docs`

**Зачем:** §4.7; примеры **curl + JS fetch** (§13 #16).

| Блок          | Содержание                               | Nest? | Статус      |
| ------------- | ---------------------------------------- | ----- | ----------- |
| Введение      | Base URL, `X-API-Key`                    | нет   | 🟡 черновик |
| Быстрый старт | Регистрация → ключ → curl → скачивание   | нет   | 🟡 черновик |
| Эндпоинты     | Таблица методов                          | нет   | ⬜          |
| Примеры       | curl и `fetch` (JetBrains Mono)          | нет   | 🟡 curl     |
| Ошибки        | HTTP + `error.code`                      | нет   | ⬜          |
| Лимиты        | 10 МБ, 60 с, rate limits §7.6            | нет   | 🟡 кратко   |
| CTA           | «Получить API-ключ» → register / account | нет   | ✅          |

Страница публичная. Контент **статический** (TSX) по контрактам ТЗ и бэкенд-плану §12 — живой Nest
для вёрстки docs не нужен; curl-примеры должны совпадать с каноном путей.

---

## 12. Глобальные UX-состояния и a11y

Пройти матрицу [technical-task.md](./technical-task.md) §5.1 и минимум §5.3:

| Тема          | Что сделать                                    | Nest? | Статус |
| ------------- | ---------------------------------------------- | ----- | ------ |
| 401 mid-flow  | Modal «Сессия истекла» → login                 | да    | ✅     |
| 429           | «Слишком много запросов. Подождите N сек»      | да    | 🟡     |
| 5xx / offline | Глобальный toast/banner                        | да    | 🟡     |
| Фокус         | Tab по кнопкам/полям; Escape в modal           | нет   | ✅     |
| Skip-link     | «Перейти к содержимому» → `<main>`             | нет   | ✅     |
| Title SPA     | `document.title` по маршруту                   | нет   | ✅     |
| 404           | «Страница не найдена» + CTA на главную         | нет   | ✅     |
| Dropzone      | `aria-label`; ошибки связаны с контролом       | нет   | ✅     |
| Адаптив       | Dropzone и CTA ≥44px hit-area; проверка mobile | нет   | ✅     |

---

## 13. Тесты

| Тип  | Инструмент | Минимальный набор v1                                                                  | Nest? | Статус      |
| ---- | ---------- | ------------------------------------------------------------------------------------- | ----- | ----------- |
| Unit | Vitest     | Валидация файла (размер, расширение, 1 файл); правила пароля                          | нет   | ⏸ пропущено |
| Unit | Vitest     | Маппинг `error.code` → RU                                                             | да    | ✅          |
| E2E  | Playwright | Гость: convert одного файла; share link open/download; register → API-ключ виден в ЛК | да    | ⏸           |

Unit по файлу и паролю — пропущено, можно вернуться отдельно. Маппинг `error.code` → RU закрыт с
этапом B. Playwright — после бэкенд-C…H
([backend-implementation-plan.md](./backend-implementation-plan.md) §15–§16).

---

## 14. Рекомендуемый порядок спринтов

Соответствие бэкенду — [backend-implementation-plan.md](./backend-implementation-plan.md) §16
(колонка «Разблокирует фронт»).

| Этап   | Содержание                | Ждёт бэкенд                             | Результат                                                | Статус |
| ------ | ------------------------- | --------------------------------------- | -------------------------------------------------------- | ------ |
| A      | §1–2, UI-kit без Dropzone | —                                       | Тёмная тема, shell, примитивы                            | ✅     |
| **A2** | **§0.1 — фронт без Nest** | —                                       | Dropzone, валидация, экраны, a11y; docs и unit пропущены | 🟡     |
| B      | §4–5                      | Бэкенд **B** (auth cookie)              | HTTP-клиент + сессия + guard                             | ✅     |
| C      | §6.3–6.6, §7 статус       | Бэкенд **C** ✅ (worker, download)      | Конвертация на главной (гость)                           | ⬜     |
| D      | §8.1                      | Бэкенд **D** (shares)                   | Share create                                             | ⏸      |
| E      | §9 данные                 | Бэкенд **E** (ключи, профиль, files)    | Живой ЛК                                                 | ⏸      |
| F      | §10.4                     | Бэкенд **G** (Telegram mock + reset)    | Recovery end-to-end                                      | ⏸      |
| G      | §12 API-ошибки, e2e       | Бэкенд **H** (429, TTL) + **I** (тесты) | Полировка, Playwright                                    | ⏸      |

Моки не используем. Этап **A2** — UI до бэкенда (docs/unit пропущены). **B** закрыт. **C**
разблокирован бэкендом. D–G — только после указанного этапа Nest. Пути не менять вразнобой:
синхронизировать оба плана (бэкенд §17).

Легенда статусов: ✅ готово · 🟡 частично (вёрстка / заглушка) · ⬜ не начато · ⏸ ждём бэкенд /
пропущено.

---

## 15. Definition of Done (фронт v1)

- [x] Все маршруты из §2 работают; Header/Footer везде
- [x] Дизайн: тёмная тема, токены DESIGN, один primary CTA на экран главной
- [ ] Гость: upload → poll → download → share (UC-01, UC-06) — после бэкенд-C/D
- [x] Auth: register/login/logout, «Запомнить меня», protected `/account`
- [x] ЛК: UI профиля, ключа, toggle, empty-списки (живые данные files/shares — после бэкенд-E)
- [ ] Recovery: формы и клиентская валидация готовы; проверка кода — после бэкенд-G
- [ ] `/api-docs` с curl + fetch, лимитами и ошибками — пропущено (пути = бэкенд §12)
- [x] A11y/адаптив без Nest: skip-link, `document.title`, 404, hit-area ≥44px
- [ ] Ошибки UI на русском по матрице §5.1 (файл — ✅; login/register — ✅; recovery — ✅; share —
      ✅; 401 mid-flow — ✅; 429/5xx обработчик готов, лимиты — после бэкенд-H)
- [ ] Unit (файл, пароль) — пропущено; Playwright — после бэкенд-C…H
- [x] Нет `X-API-Key` в localStorage; сессия только cookie
- [x] Вне scope не реализовано: batch, светлая тема, живой Telegram Bot, 2FA

---

## 16. Связанные документы

| Документ                                                           | Роль                                     |
| ------------------------------------------------------------------ | ---------------------------------------- |
| [technical-task.md](./technical-task.md)                           | Продукт и сценарии                       |
| [architecture.md](./architecture.md)                               | Слои и структура `apps/web`              |
| [tech-stack.md](./tech-stack.md)                                   | Зафиксированный стек                     |
| [DESIGN.md](./DESIGN.md)                                           | Визуальная система                       |
| [backend-implementation-plan.md](./backend-implementation-plan.md) | План `apps/api`; контракт для этапов B–G |
